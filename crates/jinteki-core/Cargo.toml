[package]
name = "jinteki-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Sans-IO Netrunner rules engine: state, cards, legality enumeration, random-walk bot"

[dependencies]
rand = "0.9"
rand_chacha = "0.9"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
